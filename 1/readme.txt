The pickle data file should be in a folder named "data" (without quotes) which is in the same folder that the notebook is.

Run the following command in you terminal before running the code:
	pip3 install -r requirements.txt